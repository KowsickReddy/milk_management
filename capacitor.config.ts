import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kowsickreddy.milkmanagement',
  appName: 'Milk Management',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
